# Frontend Client

## Theme usage
- Choose a default theme (light or dark) that matches the intended design background and update the color palette in `src/index.css` to keep foreground/background colors consistent.
- To allow users to switch themes, pass the `switchable` prop to the `ThemeProvider` in `src/App.tsx` and access the `useTheme` hook where needed.
